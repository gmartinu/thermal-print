/**
 * @thermal-print/core
 *
 * Core type definitions for thermal printer libraries
 */

// Types and interfaces (type-only)
export type { PrintNode, ElementNode, TextStyle, ViewStyle } from './types';

// Enums (these generate runtime JavaScript code)
export { StandardElementType } from './types';
